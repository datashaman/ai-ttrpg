# Branch Instead of Rewriting History

Rewinding play creates a new Timeline from a selected event position rather than deleting or rewriting accepted events. Preserving the original Timeline costs additional storage and branch-management complexity, but keeps campaign history auditable, allows either path to be resumed, and maintains the event store as an append-only source of truth.
