# Inherit the Random Stream When Branching

A new Timeline inherits the parent's random-stream position at its branch point, so repeating identical confirmed actions reproduces identical rolls rather than granting free rerolls. This limits reroll freedom but preserves deterministic comparison between Timelines and makes changed outcomes attributable to changed Player choices.
